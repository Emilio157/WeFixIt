import 'package:flutter/material.dart';

class ChatsContractor extends StatelessWidget {
  const ChatsContractor({super.key});

  @override
  Widget build(BuildContext context) {
    return 
    Center(child: Text("Chats", style: TextStyle(fontSize: 60),));
  }
}