import 'package:flutter/material.dart';

class UserPosts extends StatelessWidget {
  const UserPosts({super.key});

  @override
  Widget build(BuildContext context) {
    return 
    Center(child: Text("Publicaciones", style: TextStyle(fontSize: 60),));
  }
}